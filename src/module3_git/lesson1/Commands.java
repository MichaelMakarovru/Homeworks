package module3_git.lesson1;

class Commands {
    /*
        git init - command to initialize empty git repository. Run inside your
                    project's folder.

        git config --global user.name "Ivan Ivanov" - configure user name
        git config --global user.email "email@gmail.com" - configure user email
        git config --global --list - list global configs

        git add <file_name> - add file to tracked
        git commit -m "Commit message" - create commit
        git remote add <remote_repo_name, usually origin> <url> - add remote repo
        git push <remote_repo_name, usually origin> master - push commit to remote repo

        git checkout -b <branch_name> - create new local branch and switch
        git checkout <branch_name> - switch to other local branch

        git pull <remote_repo_name> <remote_repo_branch_name> - pull
            changes from remote repo

        git status - display current status of repo.
     */
}
